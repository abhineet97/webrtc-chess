{
  "name": "webrtc-chess",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "node lib/build-css; watchify index.js -o bundle.js -v & ecstatic",
    "build": "browserify index.js -o bundle.js; node lib/build-css.js; uglify -s bundle.js -o bundle.js; uglify -s bundle.css -o bundle.css -c"
  },
  "dependencies": {
    "chess.js": "^0.9.4",
    "peerjs": "^0.3.14"
  },
  "dev-dependencies": {
    "browserify": "^13.0.1",
    "ecstatic": "^1.4.1",
    "uglify": "^0.1.5",
    "watchify": "^3.7.0"
  }
}
